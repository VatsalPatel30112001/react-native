import React from "react";
import { StyleSheet, Text, View } from "react-native";

const Page = () => {
  return <View style={stylep.View}>{/* <Text>Page</Text> */}</View>;
};

const stylep = StyleSheet.create({
  View: {
    height: "68%",
    width: "100%",
    // backgroundColor: "blue",
  },
});

export default Page;
